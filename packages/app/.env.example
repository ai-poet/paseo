# Optional: override managed service URL (defaults to https://ai-coding.cyberspirit.io in code).
# Copy to `.env` in this directory for local Metro / Expo. See `src/config/managed-service-env.ts`.
# EXPO_PUBLIC_MANAGED_SERVICE_URL=https://your-staging-host.example
